# 영겁 → 순월 수신함

## YG-20260923-001

- 상태: `WAITING`
- 발신자: 영겁 — 집 호스트
- 수신자: 순월 — 사무실 호스트
- 최초 작성·최종 갱신: 2026-09-23 22:35 KST
- 관련 브랜치: `codex/v1-96-beta` 커밋 `75fca1a`, 배너 수정 `5fa5f8a`; Preview Ready. `main`은 같은 코드로 Production 배포 준비 중.

### 누적 작업

1. 순월의 v1.96 Preview 검증 인계를 읽고 ACK했다. 한설의 요청에 따라 AGENTS.md의 영겁·순월 말투를 공개 문서에 어색하지 않은 캐릭터 설정으로 정리했고, 영겁 자기 지칭을 통일했다.
2. 운영 RLS 정책 상세에서 계정·캐릭터·파티·문의·활동 로그·라운지·직업 테이블에 공개 쓰기 정책이 확인됐다. 관리자 카탈로그·공지·제보·캐릭터·파티·문의 쓰기를 서버 세션 경로로 옮기고, 1:1 문의 읽기도 서버로 옮겼다.
3. Phase C/D SQL과 권한 검증 쿼리를 준비했다. 운영 DB에는 **적용하지 않았다**. Phase B 역시 미적용이다.
4. 한설이 Preview에서 로그인·캐릭터 숙제 저장·파티 생성·공지 작성·문의 등록/답변·관리자 배너 등록을 확인했다. 배너는 기존 UI의 존재하지 않는 `bg_color` 필드가 실패 원인이어서 제거한 뒤 성공했다.

### 현재 상태·이어서 할 일

- 타입 검사·빌드 통과. Preview 실제 로그인 상태 쓰기 검증과 Production 배포는 미완료다.
- 새 API 및 핵심 화면 검증 후 운영 DB Phase B/C/D를 순서대로 적용하고 익명 권한을 재검사해야 20~40명 베타 초대를 판단할 수 있다.
- `main`의 v1.95는 정상 기준선이지만, 권한 SQL 적용 뒤에는 코드만 v1.95로 되돌리면 쓰기·로그인이 깨질 수 있다. DB 변경 순서를 반드시 지킬 것.

### 주의·금지

- 비밀 코드·서버 키를 문서에 기록하지 않는다. 운영 DB SQL을 검증 전 먼저 실행하지 않는다.
- 한설의 명시적인 push/DB 차단 승인은 2026-09-23 대화에서 받았지만, 코드·실화면 검증이 선행 조건이다.

### ACK

- 순월 수신 후 작성 대기.
