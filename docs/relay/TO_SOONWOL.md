# 영겁 → 순월 수신함

## YG-20260923-001

- 상태: `UPDATED`
- 발신자: 영겁 — 집 호스트
- 수신자: 순월 — 사무실 호스트
- 최초 작성: 2026-09-23 22:35 KST
- 최종 갱신: 2026-09-24 21:32 KST (v1.994 Preview 후속 탭 유지)
- 관련 브랜치: `codex/v1-96-beta` 커밋 `75fca1a`, 배너 수정 `5fa5f8a` Preview Ready; `main` v1.96 `e62d138`, v1.97 `0628582`, v1.98 `3a2318a`, v1.99 `b118f3a`, v1.991 `0966a57`, v1.992 `69443a1` Production Ready. v1.993 기존 캐릭터 저장 단축 배포 준비 중.

### 누적 작업

1. 순월의 v1.96 Preview 검증 인계를 읽고 ACK했다. 한설의 요청에 따라 AGENTS.md의 영겁·순월 말투를 공개 문서에 어색하지 않은 캐릭터 설정으로 정리했고, 영겁 자기 지칭을 통일했다.
2. 운영 RLS 정책 상세에서 계정·캐릭터·파티·문의·활동 로그·라운지·직업 테이블에 공개 쓰기 정책이 확인됐다. 관리자 카탈로그·공지·제보·캐릭터·파티·문의 쓰기를 서버 세션 경로로 옮기고, 1:1 문의 읽기도 서버로 옮겼다.
3. Phase C/D SQL과 권한 검증 쿼리를 준비했다. 이 항목은 당시의 준비 상태이고, 현재 적용 결과는 아래 5번이다.
4. 한설이 Preview에서 로그인·캐릭터 숙제 저장·파티 생성·공지 작성·문의 등록/답변·관리자 배너 등록을 확인했다. 배너는 기존 UI의 존재하지 않는 `bg_color` 필드가 실패 원인이어서 제거한 뒤 성공했다.
5. `main` 커밋 `e62d138` Production Ready. 운영 재로그인·관리자 목록 확인 후 Phase B/C/D를 SQL Editor에서 순서대로 적용했다. 익명·authenticated 쓰기 테이블 각 0, 계정 코드·해시와 문의 익명 읽기 false. 권한 차단 후 배너 수정·공지 댓글·캐릭터 저장·테스트 파티 삭제·문의/답변 조회 정상.
6. 한설이 홈 숙제 체크 저장 유지까지 약 3~5초를 관찰했다. 저장 중·완료·실패 표시와 중복 조작 방어를 후속 패치로 준비했다. 서버 왕복 지연 자체의 최적화는 남았다.
7. v1.97 `0628582` Production Ready를 한설이 확인했다. 상세 화면의 저장 표시가 보이지 않는다는 제보는 홈 전용 표시와 기존 상세 토스트의 상단 메뉴 가림 때문으로 확인했다. v1.98에서 상세 토스트를 화면 아래 중앙으로 옮기고 완료 표시를 2.5초로 연장했다. 실제 배포·화면 확인은 대기 중이다.
8. v1.98 `3a2318a` Production Ready 뒤에도 한설의 상세 화면에서 표시가 보이지 않았다. 저장은 3~5초 기다리면 유지된다. v1.99에서 `ContentChecklist` 안의 상시 상태 안내와 `document.body` 포털의 떠 있는 알림을 추가했다. 실제 화면 확인 전에는 해결로 표시하지 않는다.
9. 한설이 v1.99의 상태 표시 동작을 확인했다. 상시 안내 막대는 과해 보여 기존의 반투명 플로팅 알림만 유지하기로 했다. v1.991에서 상시 안내를 제거하고 작은 반투명 포털 알림을 유지한다.
10. v1.991 `0966a57` Production Ready 후 한설이 작은 반투명 저장 중·완료 알림을 실제 화면에서 확인했다. 집 호스트 영겁의 자기 지칭은 한설 요청에 따라 `영겁`·`영겁이 누나`·`누나`를 상황별로 자연스럽게 쓰도록 `AGENTS.md`에 갱신했다. 순월 말투·시스템 공지 문체는 변경하지 않았다.
11. 한설이 숙제 체크 저장 완료에 약 3~5초가 걸린다고 보고했다. v1.992에서 변경되지 않은 대표·기여도·계정 공통 교환 갱신 요청을 건너뛰고 자동 저장 대기를 800ms에서 500ms로 줄였다. DB 구조 변경은 없고 타입·빌드는 통과했으나 운영 속도는 아직 확인해야 한다.
12. v1.992 `69443a1` Production에서 한설이 두 체크의 저장 유지 여부를 확인했으나 완료까지 여전히 약 3~5초였다. v1.993에서는 기존 캐릭터 수정의 소유자 별도 조회를 없애고 실제 수정 쿼리에 `nickname`·로그인 계정 `owner`를 함께 걸어 한 요청으로 단축했다. 신규 등록·다른 쓰기 경로는 그대로이며 DB 구조·권한 변경 없음. 운영 실제 속도 확인은 남아 있다.
13. v1.993 `db06f7e` Production Ready 뒤 한설이 약 2~3초 저장을 확인했다. 후속 로컬 묶음에서 가입 프로필/관리자 목록, 크로노스 골드 상점·마을 임무·즐겨찾기·메모, 로고스 버그 제보·건의사항과 비공개 WebP 사진(최대 3장)을 구현했다. 한설이 계정 프로필·크로노스·로고스 SQL을 운영에서 실행하고 읽기 전용 진단을 확인했다. 로고스 제보 본인 권한은 닉네임 아닌 계정 ID 기준, 길드마스터는 전체/답변, 부마스터·대행은 타인 제보 제외다. 타입·빌드·관련 ESLint와 크로노스/로고스 320~1280px 합성 UI 테스트 통과. 실제 운영 글은 만들지 않았다. 한설이 `깃 푸시`를 요청해 v1.994 Preview 후보 브랜치로 묶되 Production은 변경하지 않는다. Preview의 실제 역할별 검증과 사진 보존/탈퇴 정책 결정은 남아 있다.
14. v1.994 Preview 후보 `2c0e378` 배포 완료. 한설이 실제 Preview에서 길드마스터 로그인, 로고스 버그 제보의 사진 3장 첨부·글 작성·답변·새로고침 후 글과 답변 유지까지 확인했다. 선택 탭이 새로고침 후 1:1 문의로 돌아오던 문제는 URL `tab` 유지로 수정했고 로컬 합성 UI·타입·빌드 통과. 같은 Preview 브랜치에 후속 push 예정이며 Production은 v1.993 그대로. 일반/부마스터 계정의 타인 제보 비노출과 사진 보존/탈퇴 정책은 미확인.

### 현재 상태·이어서 할 일

- 2026-09-24 오전 후속: 한설이 v1.993 `db06f7e` Ready와 저장 2~3초를 확인했다. 운영 가입·승인·로그인은 가능하지만 한글 직접 입력 문제를 추가 제보해 입력 UI를 보완했다. 비밀 코드 원문은 기록하지 않는다.
- 2026-09-24 저녁: `main`은 v1.993 `db06f7e`이며 v1.994는 Preview 브랜치에만 push 예정. 운영 DB의 계정 프로필·크로노스·로고스 SQL과 읽기 전용 진단은 한설이 확인했으나 새 화면의 실제 역할별 쓰기 테스트와 Preview/Production 배포 검증은 미완료. `docs/BETA_OPEN_AUDIT_20260924.md` 참고.
- 크로노스 관리자 물물교환 컬럼 불일치 수정, 새 골드 상점·마을 임무·북마크·주간 카운터·다중 탭·메모 두 슬롯을 로컬 구현했다. **운영 DB 적용/공식 push는 아직 안 했다.** SQL/확인/복구 순서는 `docs/KRONOS_MORNING_CHECKLIST.md`. 새 테이블은 기존 미확인 nexus_purchases/nexus_missions와 분리하고 기존 행은 보존한다.
- 아래 과거 대기 기록은 당시 상태다. 최신 작업은 위 항목을 우선한다. 신규 가입의 한글 타이핑과 실제 SQL 통합 확인, Tampermonkey 실동기화는 남아 있다.

- v1.992 Production 배포 및 저장 유지 확인 완료, 지연은 약 3~5초. v1.993 타입·빌드 및 무인증 401 확인, Production 배포와 속도 재확인 대기. v1.991 알림은 실제 화면 확인 완료. 운영 DB Phase B/C/D 적용 상태 유지. 신규 가입·승인의 Production 재확인과 Tampermonkey v8.3 실동기화는 후속 점검이다.
- 익명·authenticated 직접 쓰기 가능 public 테이블은 각각 0개다. 신규 가입·승인의 Production 재확인과 Tampermonkey v8.3 실동기화는 별도 후속 점검이다.
- `main`의 v1.95는 정상 기준선이지만, 권한 SQL 적용 뒤에는 코드만 v1.95로 되돌리면 쓰기·로그인이 깨질 수 있다. DB 변경 순서를 반드시 지킬 것.

### 주의·금지

- 비밀 코드·서버 키를 문서에 기록하지 않는다. 운영 DB SQL을 검증 전 먼저 실행하지 않는다.
- 한설의 명시적인 push/DB 차단 승인은 2026-09-23 대화에서 받았지만, 코드·실화면 검증이 선행 조건이다.

### ACK

- 순월 수신 후 작성 대기.
